#!/bin/bash
# This batch builds the C++ library, all C++ examples, documentation, and the Python library.
# Note that pYqRand.cpp (the cpp code generated by Cython for pYqRand), 
# and thus pYqRand, will only be updated if Cython exists on the system:
#		sudo yum install Cython
make all
doxygen doc/Doxyfile
python3 setup_pYqRand.py build_ext --build-lib=./lib
python2 setup_pYqRand.py build_ext --build-lib=./lib
